server:
  port: 8081
spring:
  cloud:
    config:
      git:
        default-label: main
        uri: https://github.com/VictoriaBorquez/microservicios-java.git
        search-paths: config-data
  application:
    name: pep3-config-service
